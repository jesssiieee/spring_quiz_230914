<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.delivery.mapper.DeliveryMapper">
 
 	<select id="selectDeliveryList" resultType="com.quiz.delivery.domain.Delivery">
 		
 		SELECT
 			`id`
			, `name`
			, `phoneNumber`
			, `address`
			, `businessNumber`
			, `introduce`
			, `createdAt`
			, `updatedAt`
		FROM
			`store`
			
 	</select>
 	
 	<select id="selectDeliveryReviewList" resultType="com.quiz.delivery.domain.DeliveryReview">
 	
 		SELECT
 			`id`
 			, `storeId`
 			, `menu`
 			, `userName`
 			, `point`
 			, `review`
 			, `createdAt`
 			, `updatedAt`
 		FROM
 			`new_review`
 	
 	</select>
 	
 	<select id="selectDeliveryJoinList" resultType="com.quiz.delivery.domain.DeliveryJoin">
 	
 		SELECT 
 			A.`name`, B.`menu`, B.`userName`, B.`point`, B.`review`
		FROM 
			`store` A, `new_review` B
		WHERE
			 A.`id` = B.`storeId`
 	
 	</select>
 
 </mapper>